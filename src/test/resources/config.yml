metric: true
checkUpdates: true

# Input here private token from key.txt(On Velocity)
privateKey: ""

# ONLY for proxy
servers:
  main: "localhost:2000"
  vanilla: "ip:port"

# for Bukkit ONLY
port: 2000